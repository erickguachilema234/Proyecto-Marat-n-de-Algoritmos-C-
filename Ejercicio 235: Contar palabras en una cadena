#include <iostream>
#include <sstream>
using namespace std;

int main() {
    string s, palabra;
    getline(cin, s);
    stringstream ss(s);
    int cont = 0;
    while (ss >> palabra) cont++;
    cout << "Palabras: " << cont;
    return 0;
}
