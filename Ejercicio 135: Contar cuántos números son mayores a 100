#include <iostream>
using namespace std;

int main() {
    int arr[8], cont = 0;
    for (int i = 0; i < 8; i++) {
        cin >> arr[i];
        if (arr[i] > 100) cont++;
    }
    cout << "Mayores a 100: " << cont;
    return 0;
}
