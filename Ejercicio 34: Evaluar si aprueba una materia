#include <iostream>
using namespace std;

int main() {
    double nota;
    cout << "Ingrese la nota del estudiante: ";
    cin >> nota;
    if (nota >= 7)
        cout << "Aprobado." << endl;
    else
        cout << "Reprobado." << endl;
    return 0;
}
